# Plan

## Current entry point (2026-09-06)

- **#177 documentation and lineage:** keep this repository as navigation, keep
  protocol/results in `cpachecker-experiments`, and keep research decisions in
  the [GitHub Wiki](https://github.com/swear01/cpachecker/wiki).
- **#180 current checkpoint preparation:** use the authenticated 218-task child
  manifest documented in
  [`HARD_218_MANIFEST_LINEAGE.md`](vguided-cegar/evaluation/HARD_218_MANIFEST_LINEAGE.md).
  The owner-authorized wave is exploratory trend evidence only; no formal timing,
  PAR-2 or publication claim follows from it.
- **Dependencies:** #178 (machine-model/runtime semantics) and #179
  (record/smoke integrity) must qualify the checkpoint. #92 remains the separate
  six-task diagnostic repair. #174 is closed by merged PR #176; that does not
  certify the six #92 cases.
- **Stopped evidence:** #173 remains a stopped/incomplete matrix; #172 is a
  completed five-fixture mechanism result. Preserve both histories and do not
  relabel crashes as UNKNOWN or solved.

## Frozen boundaries

The 224 parent is immutable. The comparable 218 cohort is exactly 224 minus the
six explicitly listed #92 tasks; the six remain an excluded diagnostic stratum.
Historical `full_scalar` (217), source-only census (245), and broad Loops
cohorts (764) have different roles and must not be called the next full
evaluation. Current production configuration uses `first_spurious` and dump
schema 12; older schedules and schema ≤9 claims are historical.

## Completed or deferred

- VGuide-NLA ordinary KI and final PDR/KI-PDR oracle-capacity gates: STOP,
  0/12 target wins; no CTI helper.
- ReachSafety v1.5–v1.7.x 764 observations: historical exploratory replays;
  prospective full764/usefulness-gate line: STOPPED.
- Precision compiler C3/C3b: bounded mechanism evidence only; no population or
  solve-rate extrapolation.
- New prompt or harness changes are outside #177.
