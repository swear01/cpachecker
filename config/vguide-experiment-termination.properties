# This file is part of CPAchecker,
# a tool for configurable software verification:
# https://cpachecker.sosy-lab.org
#
# SPDX-FileCopyrightText: 2026 Dirk Beyer <https://www.sosy-lab.org>
#
# SPDX-License-Identifier: Apache-2.0

# VGuide termination ranking-function hook experiment config (v2.0, Class-B).
# Base = lasso-only termination (isolates the lasso route for clean attribution of the hook,
# as opposed to the full svcomp27 parallel portfolio which also runs terminationToSafety).
# See docs/vguided-cegar/TERMINATION_RANKING_HOOK_PLAN.md.
#include components/termination-composition-lassoBasedAnalysis.properties

# --- VGuide ranking-function hook options ---
# NOTE: these options are READ BY the (not-yet-implemented) Java hook in LassoAnalysis.
# Keep them COMMENTED until the Java options class exists, otherwise the config fails to load
# with "unused/unknown option". Uncomment in the same change that lands the Java hook (Phase 6),
# and verify option propagation reaches LassoAnalysis through TerminationAlgorithm.
#
# termination.lassoAnalysis.vguide.enabled = true
# termination.lassoAnalysis.vguide.mode = fallback
# termination.lassoAnalysis.vguide.maxRoundsPerLoop = 1
# termination.lassoAnalysis.vguide.minIntervalSec = 0
