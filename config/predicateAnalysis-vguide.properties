# Predicate analysis with unified VGuide (includes vguide.* options)
#include predicateAnalysis.properties
#include vguide.properties

# No cpa.predicate.refinement.timelimit here (default 0 = unlimited) — matches full_scalar
# batch experiments (300s CPU). Optional hang mitigation: pass
#   --option cpa.predicate.refinement.timelimit=120000
# or set VGUIDE_INTERP_TIMELIMIT_MS in the runner (does not affect strict comparability).
